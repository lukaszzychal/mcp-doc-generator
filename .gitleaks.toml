# Gitleaks configuration for MCP Documentation Server
title = "MCP Documentation Server Secret Detection"

[extend]
useDefault = true

[allowlist]
description = "Exclude examples, tests, and documentation files with example secrets"
paths = [
    "examples/",
    "tests/",
    "docs/",
    "*.md",
]

